# Local restore and debt visibility

Portable backups restore additively into the current local SQLite workspace without authentication, network traffic, or sync work, while signed-in restores stay destination-owned and use remapped server records. Hidden debts remain ledger records and dashboard inputs, so visibility is a list preference for closed debts rather than a deletion mechanism.
